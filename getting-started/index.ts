function sum(a: number, b: number): number {
  return a + b;
}

sum(10, 20);
//compile: ts파일을 js파일로 변환하는 작업
//npm i typescript -g: 로컬에 설치
//! 터미널에 "tsc index.ts": 디렉토리에 index.js 생성 -> tsc명령어
//웹팩같은 모듈 번들러, 그런트, 걸프 등 웹자동화도구 웹 태스크매니저를 통해서 명령어를 자동화 할 수 있게됨
